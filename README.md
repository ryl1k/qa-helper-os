# QA Clipboard Helper

Chrome-розширення для швидкого копіювання відповідей до питань по хоткею **Ctrl + Shift + H**.

> 🔹 Ви виділяєте питання на сторінці → натискаєте хоткей → правильна відповідь автоматично копіюється в буфер обміну → **Ctrl + V** для вставки.

---

## 🔧 Вимоги

* Google Chrome (або Chromium-браузер)
* Доступ до `chrome://extensions`

---

## 📦 Встановлення (Load unpacked)

> ⚠️ Розширення встановлюється **локально**, без Chrome Web Store.

1. Завантажте або отримайте папку з розширенням (архів `.zip`).
2. Розпакуйте архів у будь-яку папку на комп’ютері.
3. Відкрийте Chrome та перейдіть на:

   ```
   chrome://extensions
   ```
4. Увімкніть **Developer mode** (перемикач у правому верхньому куті).
5. Натисніть **Load unpacked**.
6. Виберіть папку з розширенням (у ній має бути `manifest.json`).

✅ Після цього розширення з’явиться у списку.

---

## ⌨️ Налаштування хоткея (Ctrl + Shift + H)

1. Перейдіть на сторінку:

   ```
   chrome://extensions/shortcuts
   ```
2. Знайдіть розширення **QA Clipboard Helper**.
3. У полі команди `Find answer and copy to clipboard` встановіть:

   ```
   Ctrl + Shift + H
   ```

> ⚠️ Якщо хоткей вже зайнятий — Chrome попросить вибрати інший.

---

## ▶️ Як користуватись

1. Відкрийте будь-який сайт (не `chrome://` сторінки).
2. Виділіть текст **питання**.
3. Натисніть:

   ```
   Ctrl + Shift + H
   ```
4. Відповідь буде **скопійована в буфер обміну**.
5. Вставте її де потрібно:

   ```
   Ctrl + V
   ```

---

## 🧠 Як працює логіка

* Розширення бере виділений текст
* Нормалізує його (lowercase, без знаків пунктуації)
* Шукає відповідність у словнику `qa-map.js`
* Якщо знайдено — копіює відповідь у clipboard

---

## 📁 Структура проєкту

```
qa-clipboard-helper/
├─ manifest.json
├─ background.js
├─ content.js
├─ qa-map.js
```

* `qa-map.js` — список **питання → відповідь**
* `background.js` — логіка хоткея і копіювання
* `content.js` — отримання виділеного тексту

---

## ➕ Додавання / редагування питань

Відкрий файл `qa-map.js`:

```js
export const QA_MAP = {
  "що таке sql": "SQL — мова структурованих запитів",
  "що таке первинний ключ": "Первинний ключ — унікальний ідентифікатор запису"
};
```

Після змін:

1. Зайди в `chrome://extensions`
2. Натисни **Reload** на розширенні

---

## ❗ Обмеження

* Не працює на сторінках типу `chrome://`, Chrome Web Store
* Питання має більш-менш відповідати запису в словнику

---

## 🛠 Troubleshooting

**Нічого не копіюється:**

* Перевір, що текст справді виділений
* Переконайся, що питання є в `qa-map.js`
* Перезавантаж розширення (Reload)

**Помилка `Receiving end does not exist`:**

* Ви на забороненій сторінці (chrome://)
* Відкрий звичайний сайт і спробуй знову

---

## 📌 Підтримка

Якщо щось не працює — звертайтесь до автора репозиторію або відкривайте issue.

---

**Hotkey за замовчуванням:** `Ctrl + Shift + H`
